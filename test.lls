function getGreeting() {
  return "Hello, World!"
}

let b = getGreeting()

if (b) {
  let first = 0
  let second = 1
  let current = 0
  let idx = 0
  let a = 12
  while (idx < a) {
    idx = idx + 1
    current = first + second
    second = first
    first = current
  }
}